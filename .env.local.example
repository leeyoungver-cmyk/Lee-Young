# Admin password for /admin route. Change this to a strong value.
ADMIN_PASSWORD=changeme

# Token used by the client after login to authorize API calls.
# This MUST match ADMIN_PASSWORD on the server. The client sends this
# back in an x-admin-token header. Do not expose this in client-side code.
